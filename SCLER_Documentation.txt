SCLER - SC Localization Enhancer Russian
========================================
Руководство пользователя
Версия документации: 1.13 (2026-07-20)


1. Что делает программа
-----------------------

Эта программа добавляет в ваш файл локализации игры Star Citizen
полезную информацию о чертежах (blueprints), наградах и прогрессе,
которые можно получить за выполнение контрактов.

После её работы:
- В описаниях миссий появятся списки доступных чертежей с пометкой
  «Доступные чертежи».
- В названиях миссий появятся метки «[Чертежи]» или «[Чертежи]*».
- В описаниях появятся строки «Репутация за выполнение» и
  «Очки прогресса сценария» (опционально).
- Типы модулей в скобках будут переведены на русский язык с указанием
  класса (например, «Охладитель CIV-1A»).
- Название оружия будет дополнено размером (например, «Omnisky III Cannon (S1)»).
- Заголовки грузовых миссий будут упрощены и дополнены информацией
  о направлении, ранге и объёме груза. По умолчанию используется формат
  «Направление | Ранг | Объём». Порядок можно изменить в конфиге
  (параметр TITLE_FORMAT). При желании модуль можно отключить
  (USE_CARGO_TITLES=0).
- Возможность добавления пользовательских заметок к миссиям.
- Возможность замены любых строк через пользовательский словарь.
- Названия ракет и торпед будут дополнены типом головки
  самонаведения (CS, EM, IR).

После успешного завершения работы программы и добавления изменений
в файл локализации вы увидите на главной странице в игре слева
вверху надпись:
    Расширенная информация о наградах в заданиях

Программу нужно запускать каждый раз после обновления файла global.ini,
который обычно выпускается командой локализации после каждого патча в игре.
Файл локализации и программы для его установки могут быть скачаны из
репозитория GitHub https://github.com/n1ghter/StarCitizenRu

Если вы запустите программу при запущенной игре, то изменения в игре
вы увидите только после её перезапуска.

Программа не вмешивается в другие данные игры и не изменяет ничего,
кроме указанных строк. При первом запуске создаётся резервная копия
исходного файла global.ini (с расширением .noblueprints.bak), чтобы
можно было всё вернуть обратно.

Дисклеймер: Программа предоставляется «как есть» (AS IS). Исходный код
открыт и доступен для проверки, вредоносное содержимое отсутствует.


2. Что нужно для работы
------------------------

- Windows любой современной версии.
- PowerShell (встроен в систему, обычно уже есть).
- Права на запись в папку, где лежит программа.
- Скачанный русскоязычный файл global.ini.
- Права на запись в папку, где лежит global.ini.
- Доступ к GitHub (https://github.com/MrKraken/StarStrings) для скачивания
  файлов с описанием миссий (contracts.ini, ordnance.ini).


3. Как запустить
-----------------

Состав программы:
- SCLER_*.bat — основной запускаемый файл.
- scler.ps1 — скрипт обработки (PowerShell).
- scler_tables.ps1 — справочник программы.
- user_notes.ini — пользовательские заметки к миссиям.
- user_dict.ini — пользовательский словарь замен.
- SCLER.cfg — файл настроек (создаётся автоматически).

Все файлы должны находиться в одной папке.

1. Поместите файлы программы в удобное место.
2. Установите или обновите файл global.ini.
3. Запустите SCLER_*.bat двойным щелчком мыши.
4. Дождитесь завершения, нажмите любую клавишу.

Дополнительные ключи запуска:
  -s, --skip      Применить только пользовательские заметки и словарь,
                  без повторной обработки всего файла.
  -p, --path      Выбрать файл global.ini через диалог и сохранить
                  путь в конфиг.
  -r, --restore   Восстановить global.ini из резервной копии
                  (global.ini.noblueprints.bak).
  -?, -h, --help  Показать справку.

Примеры:
  SCLER_1.13.bat
  SCLER_1.13.bat -s "путь\к\global.ini"
  SCLER_1.13.bat -r


4. Как программа находит global.ini
------------------------------------
Программа ищет файл global.ini в следующем порядке:

1. Путь указан в аргументе командной строки.
2. Путь указан в SCLER.cfg (параметр GLOBAL_INI_PATH).
3. Файл global.ini находится в текущей папке запуска программы.
4. Файл global.ini находится в папке с программой.
5. Если файл не найден, программа предложит выбрать его через диалог.


5. Дополнительные файлы
------------------------

Файлы contracts.ini и ordnance.ini скачиваются из репозитория
https://github.com/MrKraken/StarStrings при каждом запуске.
Они содержат информацию о чертежах, наградах и типах вооружений,
добавляемых в описания миссий.
Если файлы уже есть в папке с программой, используются локальные копии.


6. Дополнительные настройки
----------------------------

Настройки хранятся в файле SCLER.cfg, который создаётся
автоматически при первом запуске.

Параметр              Описание                                                   По умолчанию
--------------------------------------------------------------------------------------------------------
USE_RP_AWARD_TAG       Информация о репутации за выполнение (1=вкл, 0=выкл)      0
USE_SPP_TAG            Информация об очках прогресса сценария (1=вкл, 0=выкл)    0
USE_COLOR_TAGS         Цветные теги предметов (1=вкл, 0=выкл)                    1
USE_USER_NOTES         Пользовательские заметки (1=вкл, 0=выкл)                  0
USE_USER_DICT          Пользовательский словарь замен (1=вкл, 0=выкл)            0
COLOR_TAGS_BLUE        Слова для синего тега (через ;)                           пусто
COLOR_TAGS_GREEN       Слова для зелёного тега                                   Grade AAA;
COLOR_TAGS_YELLOW      Слова для жёлтого тега                                    пусто
COLOR_TAGS_RED         Слова для красного тега                                   Pure;
GLOBAL_INI_PATH        Путь к global.ini (включая имя файла)                     пусто (автопоиск)
USE_CARGO_TITLES       Обогащение заголовков грузовых миссий (1=вкл, 0=выкл)     1
TITLE_FORMAT           Формат заголовков грузовых миссий (1-6)                   1
                       1 = Direction | Rank | Haul
                       2 = Direction | Haul | Rank
                       3 = Rank | Haul | Direction
                       4 = Rank | Direction | Haul
                       5 = Haul | Rank | Direction
                       6 = Haul | Direction | Rank

Примеры форматов заголовков:
  Пример для TITLE_FORMAT=1:
    Port Olisar > Lorville | СТАРШИЙ ПЕРЕВОЗЧИК | ПЕРЕВОЗКА КРУПНОГАБАРИТНОГО
  Пример для TITLE_FORMAT=3:
    СТАРШИЙ ПЕРЕВОЗЧИК | ПЕРЕВОЗКА КРУПНОГАБАРИТНОГО | Port Olisar > Lorville


7. Что вы увидите в консоли
----------------------------

Using global.ini from local file: "C:\...\global.ini"
Downloading contracts.ini...
Downloading ordnance.ini...
scler.ps1 v1.13
Using cargo titles format: Direction | Rank | Haul

Ordnance replaced:     136
Color Tags added:      2
BP Markers added:      280
Descriptions added:    255 BP, 37 MBP, 651 reputation, 62 SPP,
                       144 regional variants, 1482 item types,
                       708 module classes, 366 weapon sizes, 30 cargo titles
User data added:       1 notes, 3 dict
Total processed:       89008 lines, 3450 replacements, 1179 modified lines

Backup: C:\...\global.ini.noblueprints.bak
Done.

При запуске с ключом -s:
Using global.ini from command line: "C:\...\global.ini"
scler.ps1 v1.13

User data added:       1 notes, 3 dict
Total processed:       90121 lines, 4 replacements, 4 modified lines
Done.

При запуске с ключом -r:
Backup from: 19.07.2026 19:53
Restore global.ini from this backup? Press Y to confirm, N to cancel.
        1 file(s) copied.
Restored global.ini from backup.

---

Total processed — общее количество строк в файле.
Total replacements — общее количество применённых замен.
Total modified lines — количество уникальных изменённых строк.

Если файл уже обработан:
No modifications were necessary or possible. File global.ini kept unchanged.


8. Пользовательские заметки
----------------------------

Файл user_notes.ini позволяет добавлять собственные заметки к описаниям
миссий. Заметки будут видны в игре в описании контракта.

**Внимание:** файл user_notes.ini должен быть в кодировке ANSI (Windows-1251),
иначе в игре вы получите нечитаемый текст.

Для использования:
1. Включите флаг USE_USER_NOTES=1 в SCLER.cfg.
2. Отредактируйте файл user_notes.ini, добавив строки в формате:
     ключ_миссии = текст заметки
3. Запустите программу повторно.

Как найти нужный ключ миссии:

1. Откройте файл global.ini в текстовом редакторе.
2. Найдите строку с текстом миссии, к которой хотите добавить заметку.
3. Скопируйте часть строки до символа "=" — это ключ миссии.
4. Вставьте ключ в файл user_notes.ini, после знака "=" напишите текст
   заметки (до 256 символов). Он будет добавлен в конец описания миссии
   после строки «Заметки».

Обратите внимание: в файле может быть несколько ключей с похожим
описанием. Убедитесь, что выбрали нужный ключ.

Пример заметки в файле user_notes.ini:
TheCollector_Intro_Desc=Рога тундровых копионов могут быть найдены в тундре,
например около Shubin Mining Facility SM0-13 или MT DataCenter KH3-AAE-L.


9. Пользовательский словарь замен
----------------------------------

Файл user_dict.ini позволяет заменять любые строки в global.ini
на собственные значения.

**ВНИМАНИЕ:** Необдуманная замена строк может привести
к неработоспособности игры. Делайте замены осторожно.
При нарушении работоспособности игры восстановите оригинальный
файл global.ini из резервной копии (global.ini.noblueprints.bak).

**Внимание:** файл user_dict.ini должен быть в кодировке ANSI (Windows-1251).

Для использования:
1. Включите флаг USE_USER_DICT=1 в SCLER.cfg.
2. Отредактируйте файл user_dict.ini, добавив строки в формате:
     ключ = значение
3. Запустите программу.

Для быстрого применения только пользовательских данных
без повторной обработки всего файла используйте ключ -s:
  SCLER_1.13.bat -s

Пример замены в файле user_dict.ini:
  Исходный текст в игре: RSI Perseus
  Добавленная строка замены в user_dict.ini:
    vehicle_NameRSI_Perseus=1. RSI Perseus
  Результат в игре: 1. RSI Perseus

10. Возможные проблемы
-----------------------

Проблемы установки:
- Error: PowerShell is required — PowerShell не найден.
  Установите или включите его в компонентах Windows.
- Error: scler.ps1 not found — файл отсутствует в папке с программой.
  Переустановите программу.
- Error: scler_tables.ps1 not found — справочник отсутствует.
  Переустановите программу.

Файл global.ini:
- Error: global.ini not found. Check the file path or place the file in
  the program folder. — файл не найден ни по одному источнику.
- Error: Source file is empty / too small — файл пуст или повреждён.
  Скачайте актуальный файл локализации.
- Error: global.ini is not writable — нет прав на запись или файл
  используется другим процессом. Закройте игру, проверьте права.
- Error: Backup file not found — резервная копия не найдена при
  использовании ключа -r.

Интернет и загрузка:
- Error: Failed to download contracts.ini — нет интернета или сервер
  недоступен. Проверьте подключение или скачайте файл вручную
  с https://github.com/MrKraken/StarStrings и поместите в папку с программой.
- Error: contracts.ini is empty / does not contain required marker —
  файл повреждён. Скачайте файл заново.
- Warning: Failed to download ordnance.ini — файл не скачан, модуль
  ракет и торпед пропущен.
- Warning: ordnance.ini is empty / does not contain required marker —
  файл повреждён, модуль пропущен.

Конфигурация:
- Warning: Could not create config file. Using default settings —
  не удалось создать файл настроек. Используются стандартные параметры.
- Warning: Value for COLOR_TAGS_X is too long. Using empty —
  слишком длинное значение цветового тега. Значение сброшено.

Пользовательские данные:
- Warning: USE_USER_NOTES enabled but user_notes.ini not found —
  файл отсутствует. Отключение заметок.
- Warning: USE_USER_DICT enabled but user_dict.ini not found —
  файл отсутствует. Отключение словаря.
- Warning: note for '...' truncated to 256 characters —
  текст заметки превышает 256 символов и был обрезан.

Локализация:
- Warning: global.ini does not appear to be a Russian version.
  Программа предназначена для русского файла локализации. На английском
  файле некоторые маркеры будут переведены, но интерфейс останется
  английским.
- Warning: X ordnance keys not found in global.ini. The localization
  file may be outdated.
  ИЛИ
  Warning: X BP entries not found in global.ini. The localization
  file may be outdated.
  Файл локализации может быть устаревшим.

Ошибки записи:
- Error: Failed to create backup / write file —
  проверьте права доступа и свободное место на диске.

Прочее:
- Error: PowerShell error occurred — внутренняя ошибка PowerShell.
- Ошибки «korean_ is not recognized» или «system cannot find the path»:
  возникают из-за скобок в пути к папке korean_(south_korea).
  Разместите программу в папке без скобок, либо указывайте полный путь
  к global.ini в аргументе командной строки в двойных кавычках:
  SCLER_1.13.bat "D:\Games\RSI\StarCitizen\LIVE\data\Localization\korean_(south_korea)\global.ini"


11. Список обрабатываемых тегов
--------------------------------

Английский                                Русский
----------------------------------------------------------------------------------------
[BP], [BP]*                               [Чертежи], [Чертежи]*
Potential Blueprints                      Доступные чертежи
Multiple Blueprint Pools                  Множественные пулы чертежей
Pool N                                    Пул N
Awarded from X level variants             Награда из вариантов уровня X
[Regional Variants] example locations:    [Региональные варианты] примеры локаций:
Reputation Awarded:                       Репутация за выполнение:
Scenario Progress Points N                Очки прогресса сценария: N
Типы модулей (Cooler, Power Plant и др.)  Типы модулей с указанием класса
                                          (Охладитель CIV-1A), (Генератор щита MIL-2A)

Цветные теги предметов (Grade AAA, Pure и др.) настраиваются
в конфигурационном файле SCLER.cfg.